<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:testSpotify.ViewModels"
             NavigationPage.HasBackButton="False"
             BackgroundColor="#111"
             mc:Ignorable="d"
             x:Class="testSpotify.Views.HomePage">

    <ContentPage.BindingContext>
        <vm:HomePageViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding Artists, FallbackValue=Artists}">
                <ListView.ItemTemplate>
                    <DataTemplate >
                        <ImageCell Text="{Binding ArtistName, FallbackValue=ArtistName}"
                                   Detail="Numero di album"
                                   TextColor="White"
                                   DetailColor="PapayaWhip"
                            />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>